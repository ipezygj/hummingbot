name: Gandalfs Bridgeon:workflow_dispatch:inputs:task_name:description: 'Tehtävän nimi'required: truecode_content:description: 'Koodi jonka Gemini loi'required: truefile_path:description: 'Mihin tiedostoon koodi menee'required: truejobs:update-code:runs-on: ubuntu-lateststeps:- uses: actions/checkout@v3- name: Create or update filerun: |mkdir -p $(dirname "${{ github.event.inputs.file_path }}")echo "${{ github.event.inputs.code_content }}" > "${{ github.event.inputs.file_path }}"- name: Commit and Pushrun: |git config --global user.name "ipezygj-bot"git config --global user.email "bot@gandalf.com"git add .git commit -m "feat: ${{ github.event.inputs.task_name }} (Automated by Gandalf)"git push
