name: Gandalfs Bridge

on:
  workflow_dispatch:
    inputs:
      task_name:
        description: "Task"
        required: true
        type: string
      code_content:
        description: "Code"
        required: true
        type: string
      file_path:
        description: "Path"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Write file
        run: |
          mkdir -p "$(dirname "${{ inputs.file_path }}")"
          echo "${{ inputs.code_content }}" > "${{ inputs.file_path }}"

      - name: Commit and push
        run: |
          git config user.name "bot"
          git config user.email "bot@bot.com"
          git add .
          git commit -m "update" || exit 0
          git push
