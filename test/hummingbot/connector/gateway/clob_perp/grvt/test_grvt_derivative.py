import unittest from decimal import Decimal from hummingbot.connector.gateway.clob_perp.grvt.grvt_derivative import GrvtDerivative  class GrvtDerivativeUnitTest(unittest.TestCase):      def setUp(self):         self.trading_pairs = ["ETH-USDT"]         self.derivative = GrvtDerivative(self.trading_pairs)      def test_name(self):         self.assertEqual(self.derivative.name, "grvt")      def test_trading_pairs(self):         self.assertEqual(self.derivative._trading_pairs, ["ETH-USDT"])      def test_initialization(self):         self.assertIsNotNone(self.derivative._order_book_tracker)         self.assertIsNotNone(self.derivative._data_source)  if __name__ == "__main__":     unittest.main()
