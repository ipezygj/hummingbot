import asyncio import logging from typing import Dict, List, Optional from hummingbot.core.data_type.order_book_tracker import OrderBookTracker from hummingbot.connector.gateway.clob_perp.grvt.grvt_api_order_book_data_source import GrvtAPIOrderBookDataSource from hummingbot.logger import HummingbotLogger  class GrvtOrderBookTracker(OrderBookTracker):     _logger: Optional[HummingbotLogger] = None      @classmethod     def logger(cls) -> HummingbotLogger:         if cls._logger is None:             cls._logger = logging.getLogger(__name__)         return cls._logger      def __init__(self, connector):         self._connector = connector         super().__init__(GrvtAPIOrderBookDataSource(connector.trading_pairs), connector.trading_pairs)      async def start(self):         self._logger.info("Starting GRVT Order Book Tracker...")         await super().start()
