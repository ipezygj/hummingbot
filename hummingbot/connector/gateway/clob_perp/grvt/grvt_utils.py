from decimal import Decimal from typing import Any, Dict  from hummingbot.connector.gateway.clob_perp.grvt import grvt_constants as constants   def convert_to_exchange_trading_pair(hb_trading_pair: str) -> str:     """     Converts a Hummingbot trading pair (e.g., ETH-USDT) to GRVT format (ETH_USDT).     """     return hb_trading_pair.replace("-", "_")   def convert_from_exchange_trading_pair(ex_trading_pair: str) -> str:     """     Converts a GRVT trading pair (e.g., ETH_USDT) to Hummingbot format (ETH-USDT).     """     return ex_trading_pair.replace("_", "-")   def get_price_increment(trading_pair: str) -> Decimal:     """     Returns the minimum price increment for a given trading pair.     """     # This should be dynamic in a full implementation     return Decimal("0.01")
