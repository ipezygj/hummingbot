from decimal import Decimal from hummingbot.core.data_type.trading_pair_tuple import TradingPairTuple  # Helper to convert Hummingbot trading pair to GRVT format def convert_to_exchange_trading_pair(hb_trading_pair: str) -> str:     return hb_trading_pair.replace("-", "")  # Helper to convert GRVT trading pair back to Hummingbot format def convert_from_exchange_trading_pair(ex_trading_pair: str) -> str:     # Logic for common pairs like ETHUSDT -> ETH-USDT     if "USDT" in ex_trading_pair:         return f"{ex_trading_pair.replace('USDT', '')}-USDT"     return ex_trading_pair  class GrvtConfigMap:     # This will hold the configuration for the Hummingbot UI later     pass  def is_exchange_information_valid(exchange_info: dict) -> bool:     """     Checks if the symbol information from the exchange is complete.     """     return "symbol" in exchange_info and "pricePrecision" in exchange_info
